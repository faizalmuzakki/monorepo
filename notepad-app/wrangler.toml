name = "markdown-notepad"
compatibility_date = "2024-01-01"
pages_build_output_dir = "public"

# D1 Database binding
[[d1_databases]]
binding = "DB" # Available as env.DB in Functions
database_name = "markdown-notepad-db"
database_id = "" # Will be filled after creating D1 database

# Environment variables (optional)
[env.production]
# Add production-specific settings here

[env.preview]
# Add preview-specific settings here
